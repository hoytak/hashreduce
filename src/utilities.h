#ifndef _UTILITIES_H_
#define _UTILITIES_H_

/* A few random functions used in various parts of the code. */

#define max(a,b) ( (a) < (b) ? (b) : (a) )
#define min(a,b) ( (a) < (b) ? (a) : (b) )

#define swap(a,b) 

#endif /* _UTILITIES_H_ */
